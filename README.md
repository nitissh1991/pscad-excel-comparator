# Two-File Overlay Plotter (Excel + CSV)

Upload two files (Excel or CSV), select X(time) and Y columns, and overlay plots.

## Run locally
```bash
pip install -r requirements.txt
streamlit run app.py
```

## Deploy (Streamlit Community Cloud)
- Push this repo to GitHub
- On Streamlit Cloud: New app -> select repo -> main -> app.py